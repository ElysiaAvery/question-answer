
import { mostVote } from 'question-answer/helpers/most-vote';
import { module, test } from 'qunit';

module('Unit | Helper | most vote');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mostVote([42]);
  assert.ok(result);
});

